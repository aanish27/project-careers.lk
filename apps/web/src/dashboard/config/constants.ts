export const ACCESS_TOKEN_TTL_MS = 15 * 60 * 1000; // mirrors the backend's JWT_ACCESS_TOKEN_EXPIRES_IN
export const SESSION_MAX_AGE_MS = 7 * 24 * 60 * 60 * 1000; // mirrors the backend's JWT_REFRESH_TOKEN_EXPIRES_IN
export const REFRESH_THRESHOLD_MS = 60 * 1000;
export const SESSION_COOKIE_NAME = "admin_session";
